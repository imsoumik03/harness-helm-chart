{{- if .Values.serviceAccount.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "idp-admin.name" . }}-additional-role-binding
  namespace: {{ .Release.Namespace }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "idp-admin.name" . }}-additional-role
subjects:
  - kind: ServiceAccount
    name: {{ include "idp-admin.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}